/**
 * Central store for UI text constants used in assertions.
 * Update these values if the application changes its displayed messages.
 */
export const TEXT = {
  profile: {
    /** Substring of the success message shown after saving the profile. */
    success: 'Cập nhật thành công',
  },
} as const;

